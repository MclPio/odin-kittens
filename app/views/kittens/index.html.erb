<h4>🐱Kitten List🐱</h4>
<ul>
  <% @kittens.each do |kitten| %>
    <li>
      <%= link_to kitten.name, kitten_path(kitten.id) %>
      <%= link_to "Delete", kitten_path(kitten), data: {
        turbo_method: :delete,
        turbo_confirm: "Are you sure?"
      } %>
    </li>
  <% end %>
</ul>
<%= link_to "New Kitten", new_kitten_path %>