<% if @kitten.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@kitten.errors.count, "error") %> prohibited this kitten from being saved:</h2>

    <ul>
      <% issues = [] %>
      <% @kitten.errors.each do |error| %>
        <% filtered_message = error.full_message if error.options[:message] %>
        <% issues << filtered_message if !filtered_message.nil? %>
      <% end %>
      <% issues.each do |issue| %>
        <li><%= issue %></li>
      <% end %>
    </ul>
  </div>
<% end %>
